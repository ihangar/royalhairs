function format_money(input){    var output = '';    input = input.toString();    while (input.length >= 3){      output = ' ' + input.substr(input.length - 3, 3) + output;      input = input.substr(0, input.length - 3);    }    return input + output;};$(document).ready(function(){  // пересчет цены товара  $('.item-count').change(recalc_item).change();  function recalc_item(){  alert('1');  /*    $info = $(this).closest('.product');    $info.find('.item-add').removeClass('item-checkout');        var title = $info.find('.item-title').val().split('|')[0];    var price = $info.find('.item-title').val().split('|')[1];    var count = $info.find('.item-count').val();        var total = price * count;    var $total = $info.find('.item-total');    $total.animateNumber({      number: total, easing: 'easeOutExpo',      numberStep: $.animateNumber.numberStepFactories.separator(' ', 3, ' руб.')    });    $total.prop('number', total);    */  }    recalc_item();});